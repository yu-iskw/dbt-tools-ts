# Copyright 2026 yu-iskw
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Publish @dbt-tools

on:
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: publish
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    name: Validate @dbt-tools packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # ratchet:step-security/harden-runner@v2.19.4
        with:
          egress-policy: audit
      - name: Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # ratchet:pnpm/action-setup@v6.0.8
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # ratchet:actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: '@dbt-tools'
      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
      - name: Build @dbt-tools packages
        run: |
          pnpm --filter @dbt-tools/core build
          pnpm --filter @dbt-tools/cli build
          pnpm --filter @dbt-tools/mcp build
          pnpm --filter @dbt-tools/web build
      - name: Run tests
        run: |
          pnpm --filter @dbt-tools/core test
          pnpm --filter @dbt-tools/cli test
          pnpm --filter @dbt-tools/mcp test
      - name: Require npm CLI for OIDC trusted publishing
        run: |
          set -euo pipefail
          v=$(npm --version)
          echo "npm version: ${v}"
          IFS='.-' read -r major minor patch _ <<<"${v}"
          major=${major:-0}
          minor=${minor:-0}
          patch=${patch:-0}
          if [ "${major}" -lt 11 ] \
            || { [ "${major}" -eq 11 ] && [ "${minor}" -lt 5 ]; } \
            || { [ "${major}" -eq 11 ] && [ "${minor}" -eq 5 ] && [ "${patch}" -lt 1 ]; }; then
            echo "::error::npm must be >= 11.5.1 for trusted publishing (https://docs.npmjs.com/trusted-publishers)"
            exit 1
          fi

  publish-core:
    name: Publish @dbt-tools/core to npm
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
      id-token: write
    environment:
      name: release
      deployment: true
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # ratchet:step-security/harden-runner@v2.19.4
        with:
          egress-policy: audit
      - name: Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # ratchet:pnpm/action-setup@v6.0.8
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # ratchet:actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: '@dbt-tools'
      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
      - name: Publish to npm
        run: |
          pnpm publish --filter @dbt-tools/core --no-git-checks --access public

  publish-cli:
    name: Publish @dbt-tools/cli to npm
    runs-on: ubuntu-latest
    needs: publish-core
    permissions:
      contents: read
      id-token: write
    environment:
      name: release
      deployment: true
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # ratchet:step-security/harden-runner@v2.19.4
        with:
          egress-policy: audit
      - name: Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # ratchet:pnpm/action-setup@v6.0.8
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # ratchet:actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: '@dbt-tools'
      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
      - name: Build @dbt-tools/core
        run: |
          pnpm --filter @dbt-tools/core build
      - name: Publish to npm
        run: |
          pnpm publish --filter @dbt-tools/cli --no-git-checks --access public

  publish-web:
    name: Publish @dbt-tools/web to npm
    runs-on: ubuntu-latest
    needs: publish-core
    permissions:
      contents: read
      id-token: write
    environment:
      name: release
      deployment: true
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # ratchet:step-security/harden-runner@v2.19.4
        with:
          egress-policy: audit
      - name: Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # ratchet:pnpm/action-setup@v6.0.8
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # ratchet:actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: '@dbt-tools'
      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
      - name: Publish to npm
        run: |
          pnpm publish --filter @dbt-tools/web --no-git-checks --access public

  publish-mcp:
    name: Publish @dbt-tools/mcp to npm
    runs-on: ubuntu-latest
    needs: publish-core
    permissions:
      contents: read
      id-token: write
    environment:
      name: release
      deployment: true
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # ratchet:step-security/harden-runner@v2.19.4
        with:
          egress-policy: audit
      - name: Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # ratchet:pnpm/action-setup@v6.0.8
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # ratchet:actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: '@dbt-tools'
      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
      - name: Build @dbt-tools/core
        run: |
          pnpm --filter @dbt-tools/core build
      - name: Publish to npm
        run: |
          pnpm publish --filter @dbt-tools/mcp --no-git-checks --access public
