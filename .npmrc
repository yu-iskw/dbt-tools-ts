# RFC-0001 §8.1 — block lifecycle scripts during install (pnpm honors this alongside allowBuilds).
ignore-scripts=true

# Some registry mirrors omit publish timestamps for transitive packages. Prefer the highest
# matching version instead of failing minimumReleaseAge evaluation with ERR_PNPM_MISSING_TIME.
resolution-mode=highest
