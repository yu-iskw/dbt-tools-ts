/** Shown when the server session is configured but artifact bytes cannot be read. */
export const MANAGED_ARTIFACT_BYTES_ERROR = 'Could not read artifact bytes from the server.';

export const DEFAULT_REMOTE_POLL_INTERVAL_MS = 30_000;
